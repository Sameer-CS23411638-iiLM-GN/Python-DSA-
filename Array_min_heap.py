# for in in range(n//2,-1,-1):
#     L_child = (2*in)+1
#     R_child = (2*in)+2
#     if L_child < n and arr[L_child] < arr[in]:
#         return False
#     if R_child < n and arr[R_child] < arr[in]:
#         return False
# return True